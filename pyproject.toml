[tool.ruff]
line-length = 92
exclude = [".venv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
# Long lines inside CUDA/C++ kernel strings cannot be broken
"PolicyIterationStall.py" = ["E501"]
